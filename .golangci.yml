version: "2"

run:
    timeout: 5m

linters:
    default: standard
    enable:
        - bodyclose
        - errorlint
        - usestdlibvars
        - wsl_v5
        - funlen
        - nestif
        - cyclop
        - gocognit

formatters:
    enable:
        - gofumpt
